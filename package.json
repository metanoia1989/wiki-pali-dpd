{
  "name": "wiki-pali-dpd",
  "version": "0.2.0",
  "description": "Tampermonkey userscript for Wiki Pali - adds declension tables and compound word deconstruction using DPD data",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-guide.js",
    "build:guide": "node scripts/build-guide.js",
    "preview": "vite preview",
    "test": "python3 -m http.server 8080 -d .",
    "guide": "python3 -m http.server 8080 -d ./dist",
    "export-db": "python3 scripts/export/web_db.py --src ~/WorkSpace/dpd-db/dpd.db",
    "get-db": "cp data/dpd-web.db.gz ./dist/dpd-web.db.gz"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "marked": "^15.0.0",
    "vite": "^5.4.0 || ^6.0.0",
    "wrangler": "^4.106.0"
  }
}
